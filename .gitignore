# Mac OS X files
.DS_Store

# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Project-local glide cache, RE: https://github.com/Masterminds/glide/issues/736
.glide/

# Moc (Qt Meta Object Compiler) generated files
*moc*.*
internal/ui/*moc*.*

# Qt Resource Compiler generated files
*rcc*.*
internal/ui/*rcc*.*

build
vendor
cmd/clefui/darwin/
cmd/clefui/deploy/
.idea
*.iml
audit.log
